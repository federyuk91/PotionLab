# Sprite Toolbox Quick Start

1. Open `QuickStartSpriteDocument.asset` with `Tools > Sprite Toolbox > Pixel Editor`.
2. Inspect its four 78x78 frames; the logo uses a subtle vertical bob.
3. Edit a frame, then use `File > Export > Sprite Sheet` to create a PNG, sliced sprites, and an animation clip.
